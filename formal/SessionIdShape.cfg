\* Proof config: the shape guard refuses both epoch widths while keeping every
\* real counter and every clone prefix. Expect: no error.
CONSTANTS
  MaxDigits      = 9
  MaxRealCounter = 9
  EpochSecDigits = 10
  EpochMsDigits  = 13
SPECIFICATION Spec
INVARIANT AllProps
