\* Refutation config: the single-step (increment-by-one) migrator is not total.
\* Expect: TLC reports INVARIANT NaiveReach violated + a counterexample
\* (e.g. ver = 0 migrates to 1, not CURRENT=3).
CONSTANTS
  CURRENT = 3
SPECIFICATION Spec
INVARIANT NaiveReach
