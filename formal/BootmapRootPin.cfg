\* Proof config: boot_root is pinned to ProjectRoot, decoupled from cwd/gc-path.
\* Expect: no error.
CONSTANTS
  ProjectRoot = ProjectRoot
  OtherDir    = OtherDir
  NoPath      = NoPath
SPECIFICATION Spec
INVARIANT AllProps
