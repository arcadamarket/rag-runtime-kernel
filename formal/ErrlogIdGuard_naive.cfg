\* Refutation config: the NAIVE (count-all-headings) guard is unsound.
\* Expect: TLC reports INVARIANT NaiveFalsePos violated + a counterexample
\* (a legitimate Dfn+Rcr log the naive guard wrongly rejects).
CONSTANTS
  Ids = {E1, E2}
  Dfn = Dfn
  Rcr = Rcr
  Mfd = Mfd
  MaxLen = 4
SPECIFICATION Spec
INVARIANT NaiveFalsePos
