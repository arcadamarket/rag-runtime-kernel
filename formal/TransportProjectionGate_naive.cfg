\* Refutation config: existence-only checking ("a projection file is present, so
\* we are fine") misses a projection that is present, parseable, and rendered
\* from a rule text that has since changed. Expect: TLC reports
\* NaiveCatchesDrift VIOLATED. A pass here means the gate has been weakened
\* back to a file-exists test.
SPECIFICATION Spec
INVARIANT NaiveCatchesDrift
