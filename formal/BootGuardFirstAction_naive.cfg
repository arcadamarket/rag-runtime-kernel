\* Refutation config: the NAIVE (Boot-appears-somewhere) guard is unsound.
\* Expect: TLC reports INVARIANT NaiveSound violated + a counterexample
\* (trace <<Work, Boot>>: a governed Work action ran before session-start).
CONSTANTS
  Boot   = Boot
  Work   = Work
  MaxLen = 5
SPECIFICATION Spec
INVARIANT NaiveSound
