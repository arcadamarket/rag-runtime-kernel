\* Refutation config: naive single-check gates are unsound.
\* Expect: TLC reports NaiveIdSound (or NaiveTextSound) violated with a
\* counterexample -- [idBound|->TRUE, restatementExact|->FALSE] for the
\* ID-only gate, or [idBound|->FALSE, restatementExact|->TRUE] for text-only.
SPECIFICATION Spec
INVARIANT NaiveIdSound
INVARIANT NaiveTextSound
