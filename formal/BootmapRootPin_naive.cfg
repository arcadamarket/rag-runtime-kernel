\* Refutation config: the gc-path/CWD-keyed boot_root is not pinned.
\* Expect: TLC reports INVARIANT NaivePinned violated + a counterexample
\* (e.g. [cwd |-> OtherDir, gc |-> NoPath] resolving boot_root off the CWD).
CONSTANTS
  ProjectRoot = ProjectRoot
  OtherDir    = OtherDir
  NoPath      = NoPath
SPECIFICATION Spec
INVARIANT NaivePinned
