\* Refutation config: the pre-S198 rule ("it ends in digits, so increment it")
\* admits both epoch widths, including the exact phantom S1786488555313.
\* Expect: TLC reports NaiveNoEpoch VIOLATED. A pass here means the guard has
\* been silently reverted.
CONSTANTS
  MaxDigits      = 9
  MaxRealCounter = 9
  EpochSecDigits = 10
  EpochMsDigits  = 13
SPECIFICATION Spec
INVARIANT NaiveNoEpoch
